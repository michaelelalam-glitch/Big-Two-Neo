# Flow 60: Match History Screen — Scroll and verify
# Validates: Match history renders, scrollable list visible, no crash
# Tags: ci-authenticated, navigation, match-history

appId: com.stephanos.app
tags:
  - ci-authenticated
  - navigation
  - match-history
---
- launchApp

# Dismiss consent modal if shown (defensive CI fallback — R32)
- runFlow: ../shared/dismiss_consent_if_shown.yaml
- extendedWaitUntil:
    visible: "Choose a game to play"
    timeout: 35000
- tapOn:
    id: "lebanese-poker-card"
- extendedWaitUntil:
    visible: "Stephanos"
    timeout: 10000

# Navigate to match history (via Profile or dedicated nav)
- tapOn:
    id: "home-profile-button"
- extendedWaitUntil:
    visible:
      id: "profile-screen"
    timeout: 8000

# Tap Match History tab/button
- tapOn: "Match History"
- takeScreenshot: "60_match_history_screen"

# Scroll through history
- swipe:
    direction: DOWN
    duration: 800
- takeScreenshot: "60_match_history_scrolled"

- swipe:
    direction: UP
    duration: 800

- back
- extendedWaitUntil:
    visible:
      id: "profile-screen"
    timeout: 8000
- back
- extendedWaitUntil:
    visible: "Stephanos"
    timeout: 8000
