# Flow 04: Create Room Flow
# Validates: User can navigate to Create Room and create a private room
# Tags: rooms, critical-path
#
# Prerequisites: User is authenticated
# Source: CreateRoomScreen.tsx — verified against real UI screenshots

appId: com.big2mobile.app
tags:
  - rooms
  - critical-path
---
# Step 1: Launch and navigate to Home
- launchApp
- extendedWaitUntil:
    visible: "Choose a game to play"
    timeout: 15000
- tapOn: "PLAY →"
- extendedWaitUntil:
    visible: "Big2 Mobile"
    timeout: 10000

# Step 2: Tap "Create Room"
- tapOn: "Create Room"

# Step 3: Verify Create Room screen
- extendedWaitUntil:
    visible: "Create a private room and invite your friends"
    timeout: 10000
- assertVisible: "Up to 4 players can join"
- assertVisible: "Fill empty slots with bots"

# Step 4: Take screenshot
- takeScreenshot: "04_create_room_screen"

# Step 5: Tap "Create Room" button (the action button, not the header)
- tapOn:
    text: "Create Room"
    index: 1

# Step 6: Verify navigation to Lobby
- extendedWaitUntil:
    visible: "Game Lobby"
    timeout: 15000
- assertVisible: "Game Lobby"
- assertVisible: "Room Code:"
- assertVisible: "Players"
- assertVisible: "Empty Slot"

# Step 7: Take screenshot of lobby
- takeScreenshot: "04_lobby_created"
