name: CI/CD Pipeline

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  test:
    name: Lint, Type-Check, and Test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    strategy:
      matrix:
        node-version: [20.x]
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🔧 Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      
      - name: 📦 Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10
      
      - name: 🔍 Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      
      - name: 💾 Setup pnpm cache
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      
      - name: 💾 Setup Jest transform cache
        uses: actions/cache@v3
        with:
          path: apps/mobile/.jest-cache
          key: ${{ runner.os }}-jest-${{ hashFiles('apps/mobile/src/**/*.ts', 'apps/mobile/src/**/*.tsx') }}
          restore-keys: |
            ${{ runner.os }}-jest-
      
      - name: 📦 Install dependencies
        run: |
          cd apps/mobile
          pnpm install --frozen-lockfile
      
      - name: 🔍 Run ESLint
        run: |
          cd apps/mobile
          pnpm run lint || echo "::warning::ESLint reported warnings (non-blocking)"
      
      - name: 🔧 Run TypeScript type-check
        run: |
          cd apps/mobile
          npx tsc --noEmit
      
      - name: 🧪 Run unit tests + coverage
        run: |
          cd apps/mobile
          # Single test run with coverage enabled.
          # Previously split into two steps (unit + coverage) which doubled execution
          # time on CI — the transform cache was cold each run, causing 15+ min compiles.
          # Now combined: exit code is the authoritative pass/fail signal (audit N1),
          # and coverageThreshold in jest.config.js enforces minimum coverage.
          # --forceExit: forces shutdown after tests complete (open-handle safety net).
          # --maxWorkers=2: matches CI runner's 2 vCPUs.
          NODE_OPTIONS="--max-old-space-size=3072" npx jest \
            --testPathIgnorePatterns='/integration/' \
            --forceExit --passWithNoTests \
            --maxWorkers=2 \
            --testTimeout=15000 \
            --coverage
        timeout-minutes: 12
      
      
      - name: ⚠️ Integration tests skipped (no credentials)
        if: env.EXPO_PUBLIC_SUPABASE_ANON_KEY == '' || env.SUPABASE_SERVICE_ROLE_KEY == ''
        run: |
          echo "::warning::Integration tests cannot run because EXPO_PUBLIC_SUPABASE_ANON_KEY and/or SUPABASE_SERVICE_ROLE_KEY are not configured as GitHub secrets."
          echo "Add these secrets to the repository to enable integration tests."
          echo "Skipping integration tests — they are non-blocking until secrets are configured."

      - name: 🧪 Run integration tests
        if: env.EXPO_PUBLIC_SUPABASE_ANON_KEY != '' && env.SUPABASE_SERVICE_ROLE_KEY != ''
        run: |
          cd apps/mobile
          # Integration tests are blocking when credentials are present.
          # Exit code is the authoritative pass/fail signal.
          NODE_OPTIONS="--max-old-space-size=3072" npx jest \
            --testPathPattern='integration/' \
            --forceExit --passWithNoTests \
            --maxWorkers=2 \
            --testTimeout=30000 \
            --no-coverage
        timeout-minutes: 5
        env:
          EXPO_PUBLIC_SUPABASE_URL: ${{ secrets.EXPO_PUBLIC_SUPABASE_URL }}
          EXPO_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.EXPO_PUBLIC_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          CI: true
      
      - name: 📊 Upload test coverage
        uses: codecov/codecov-action@v3
        if: always()
        with:
          directory: ./apps/mobile/coverage
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false
        continue-on-error: true
      
      - name: ✅ All checks passed
        run: echo "🎉 All CI checks completed successfully!"

  build:
    name: Build Check
    runs-on: ubuntu-latest
    needs: test
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
      
      - name: 📦 Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10
      
      - name: 📦 Install dependencies
        run: |
          cd apps/mobile
          pnpm install --frozen-lockfile
      
      - name: 🏗️ Check Expo build configuration
        run: |
          cd apps/mobile
          npx expo prebuild --no-install --clean
      
      - name: ✅ Build configuration valid
        run: echo "🎉 Build configuration is valid!"
